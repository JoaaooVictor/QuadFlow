name: QuadFlow CI
on:
  push:
    branches:
      - master
jobs: 
  build:
    runs-on: ubuntu-latest

    steps: 
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore QuadFlow.sln

      - name: Build
        run: dotnet build QuadFlow.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test QuadFlow.sln --no-build --configuration Release